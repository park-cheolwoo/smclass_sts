<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.dao.CustomerMapper">

  <select id="selectAll" resultType="com.java.dto.BoardDto">
     select * from board order by bgroup desc,bstep asc
  </select>
  
  <select id="selectOne" resultType="com.java.dto.BoardDto">
     select * from board where bno=#{bno}
  </select>
  
  <select id="prevSelect" resultType="com.java.dto.BoardDto">
	 	select * from board where bno=
		(select prev from
		(select lag(bno,1,-1) over(order by bgroup desc, bstep asc) as prev,bno from board a where prev=a.#{bno}) 
  </select>
  
  
  
</mapper>